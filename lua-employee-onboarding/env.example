# Example environment variables file
# Copy this to .env and update with your actual API keys

# Lua API Key (optional - fallback if not in keychain)
# Use this for CI/CD environments or if you prefer .env over keychain
# Priority: 1. Keychain, 2. Environment variable, 3. .env file
# LUA_API_KEY=your-lua-api-key-here

# OpenAI API Key (for AI tools)
OPENAI_API_KEY=your-openai-api-key-here

# Pinecone API Key (for vector search tools)  
PINECONE_API_KEY=your-pinecone-api-key-here

# Stripe API Key (for payment tools)
STRIPE_SECRET_KEY=your-stripe-secret-key-here

# Brex API Token (for financial onboarding)
# Get your API token from: https://developer.brex.com/
BREX_API_TOKEN=your-brex-api-token-here

# Slack Bot Token (for employee onboarding communications)
# Get your bot token from: https://api.slack.com/apps
# Required scopes: chat:write, users:read, reactions:write, im:write
SLACK_BOT_TOKEN=your-slack-bot-token-here

# Airtable API Token (for database access)
# Get your token from: https://airtable.com/create/tokens
AIRTABLE_TOKEN=your-airtable-api-token-here

# Airtable Workspace ID (for creating new bases)
# Find in URL: https://airtable.com/wsp{WORKSPACE_ID}/...
# Optional - only needed if creating new bases
AIRTABLE_WORKSPACE_ID=wspXXXXXXXXXXXXXX

# Note: Channel configuration (like onboarding channel) comes from lua-whatsapp Slack config
# Note: Employee data (birthdays, etc.) stored in resources uploaded via Lua dashboard

# Custom application settings
BASE_URL=http://localhost:3000
API_KEY=your-api-key-here

# Note: Values in lua.skill.yaml will override values in this .env file
